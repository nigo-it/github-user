import { StyleSheet } from 'react-native'

const styles = StyleSheet.create(
  {
    container: {
      flex: 1,
      marginTop: 10,
      alignItems: 'center',
    },
    image: {
      height: 250,
      width: 300,
    }
  }
)

export default styles
